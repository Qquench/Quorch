# 2026-09-24_v1.08_step02_lease_heartbeat_and_dual_process_cas Development Tasks / 开发任务单

> **Execution Guidelines for AI Models / 执行模型须知**
> - Strictly follow each task's [Step-by-Step Instructions / 分步改造指引] in sequential order
> - Do not modify files outside the declared task scope / 不得修改任务未涉及的文件
> - Preserve all existing comments and docstrings unless explicitly instructed / 保留所有现有注释和文档字符串
> - **Mandatory Unit Test Assertions / 改逻辑必加单测断言**：Append assertions in the test directory to prevent regressions
> - Upon starting a task, update its status to `🔨 执行中`; upon completion, update to `✔️ 已完成`

- **Created Date / 创建日期**：2026-09-24

---

## Task List & Status / 任务清单与状态

### 任务 2.1 ✔️ 已完成 — 隐式与显式租约心跳刷新及工作区沉浸防杀探针

#### 【涉及文件】
```
[NEW]    plugins/quench-dev-tasks/server/manifest_lease.py
[MODIFY] plugins/quench-dev-tasks/server/reaper.py
[NEW]    plugins/quench-dev-tasks/server/tests/test_lease_heartbeat_external.py
```

#### 【缺陷根因与修改目标】
```
【根因分析】
外部 CLI 执行耗时较长的大规模测试或重构时，不会主动调用 MCP 工具，导致租约 TTL 超时被后台
Reaper 强行回收。manifest.py 的 touch_heartbeat (L409) 已有原子续约原语，但其签名要求调用
方提供 generation/holder_token，外部进程难以低成本获取，需要封装高层 API。

【修改目标】
1. 新建 manifest_lease.py，提供面向外部调用方的高层心跳 API：
   - touch_lease_heartbeat(workspace_root, session_id, ttl_seconds) -> bool
     ► 仅当 session_id ∧ holder_token ∧ generation 三者锁内同时一致时才委托
       manifest::touch_heartbeat 完成原子续约，不在 manifest_lease.py 重复持锁；
     ► ttl_seconds 须在 (0, MAX_TTL] 范围内，越界抛 LeaseContractError；
   - is_workspace_actively_modifying(workspace_root, files, window_seconds=300, *, max_scan=256)
     ► 纯读函数；越界/穿越路径静默剔除；全部 stat 失败时返回 True（保守存活，防误杀）；
       命中首个新鲜 mtime 即早退；
2. 在 reaper.py::probe_lease_health 判定矩阵注入防杀前置条件：文件活跃时 hb_stale 不触发
   STALE_SUSPECT，降级为 HEALTHY；
3. 代码注释中显式声明：fail-closed 校验发生在 touch_heartbeat 临界区内且同时校验
   holder_token 与 generation；manifest_lease 侧读不持锁但不得缓存结果（避免 TOCTOU）。

【已知局限声明（须写入代码注释）】
长时间纯计算/网络等待（>window_seconds）且不留白名单文件 mtime 时，hb_stale ∧ mtime_stale
两套机制同时失效，仍会触发 STALE_SUSPECT。此为设计已知局限，须在注释中标注。

【依赖关系】
本任务与任务 2.2 无执行顺序约束，可并行。本任务须在任务 2.3 之前完成。
```

#### 【目标签名与类型契约】
```python
# manifest_lease.py（新建文件）
from typing import Sequence

class LeaseContractError(Exception):
    """租约契约校验失败（参数越界、session 映射不存在等），调用方须在发起前修正。"""

MAX_TTL: int = 3600  # 秒，可由项目配置覆写

def is_workspace_actively_modifying(
    workspace_root: str,
    files: Sequence[str],
    window_seconds: int = 300,
    *,
    max_scan: int = 256,
) -> bool:
    """检查工作区白名单文件中，是否存在 mtime 距 now 在 window_seconds 内的文件。
    - 进入循环前对每个 path 做 resolve() + is_relative_to(workspace_root) 校验；
      越界/穿越项静默剔除。
    - 全部 stat 失败 / 非 OSError：返回 True（保守存活，防误杀），stderr 告警。
    - 命中首个新鲜 mtime 即短路返回 True（early-exit）。
    - 扫描数不超过 max_scan（超出截断，记录告警）。
    """

def touch_lease_heartbeat(
    workspace_root: str,
    session_id: str,
    ttl_seconds: int,
) -> bool:
    """面向外部调用方的显式心跳刷新。
    - ttl_seconds 须满足 0 < ttl_seconds <= MAX_TTL，否则抛 LeaseContractError。
    - 锁内同时校验 session_id ∧ holder_token ∧ generation 三者一致时续约；
    - 任何不一致：fail-closed 返回 False；
    - 读取 generation/holder_token 后立即委托，不得缓存（避免 TOCTOU）。
    """
```

#### 【分步改造指引】
1. 新建 manifest_lease.py，实现 LeaseContractError 和 is_workspace_actively_modifying：路径规范化 + max_scan 截断 + 全部失败返回 True；
2. 实现 touch_lease_heartbeat：校验 ttl_seconds 范围；锁内三者同时校验；仅一致时委托 manifest.touch_heartbeat；
3. 在 reaper.py 导入 is_workspace_actively_modifying，在 probe_lease_health 判定矩阵注入防杀逻辑；
4. 代码注释标注已知局限；
5. 编写 test_lease_heartbeat_external.py，**必含断言**：全部 stat 失败返回 True；越界路径被剔除；TTL 越界抛 LeaseContractError；session_id 不匹配返回 False；token/generation 任一不符返回 False；mtime 防杀拦截降级为 HEALTHY；
6. DoD 须包含全量套件回归。

#### 【防御与边缘校验】
- files 越界/穿越项静默剔除，全部失败返回 True + stderr 告警
- `0 < ttl_seconds <= MAX_TTL`，越界抛 LeaseContractError
- touch_lease_heartbeat 不缓存 generation/holder_token（避免 TOCTOU）
- 续约仅当三者锁内同时一致时触发（fail-closed）
- max_scan 防无限扫描拖慢 Reaper 主循环

#### 【DoD 验证命令】
```bash
python -m pytest plugins/quench-dev-tasks/server/tests/test_lease_heartbeat_external.py -q
python -m pytest plugins/quench-dev-tasks/server/tests/ -q   # 全量套件，20/20 零回归
```

---

### 任务 2.2 ✔️ 已完成 — 锁内重读 CAS (Read-Under-Lock CAS) 与双进程并发加固

#### 【涉及文件】
```
[MODIFY] plugins/quench-dev-tasks/server/manifest.py
[NEW]    plugins/quench-dev-tasks/server/tests/test_dual_process_cas.py
```

#### 【缺陷根因与修改目标】
```
【根因分析】
manifest.py 中五个函数各自独立持锁 + load_manifest + atomic_replace_manifest，大量重复且
无统一事务标识。进程在持锁间隙若依赖内存缓存状态，易发生 TOCTOU 竞争漏洞与脏写覆盖。

【修改目标】
1. 新增 RetryableManifestError 基类和 ManifestConflictError 异常类：
   ⚠️ 禁止继承 ManifestIntegrityError（Fatal 分支），避免 fatal 处理器误捕获可恢复冲突：
     class RetryableManifestError(Exception): ...
     class ManifestConflictError(RetryableManifestError): ...

2. 新增 mutate_manifest_under_lock 通用事务上下文：
   - mutator 必须为纯函数，禁止调用任何 manifest 取锁 API（违反 → RuntimeError，非死锁）；
   - mutator 抛异常时：跳过 atomic_replace + 释放锁(finally) + 关闭 fd(finally)，原样重抛；
   - helper 内部 fresh-fd 读时记录 generation，replace 前再断言未变（兜底 CAS）；
   - 使用 TypeVar 而非 PEP 695 [T] 语法（兼容 Python < 3.12）；

3. 将五函数持锁 + 重读 + 写回逻辑迁移至 mutate_manifest_under_lock，保持外部签名、返回类型
   与异常类型完全不变；

4. 编写 test_dual_process_cas.py 使用 multiprocessing.Barrier 强制交叠，断言零脏写。

【依赖关系】
本任务与任务 2.1 无执行顺序约束；touch_lease_heartbeat 依赖 touch_heartbeat 签名不变。
本任务须在任务 2.4 之前完成。
```

#### 【目标签名与类型契约】
```python
# manifest.py 新增内容
from typing import Callable, Tuple, TypeVar
T = TypeVar("T")  # TypeVar 兼容 Python < 3.12，非 PEP 695 [T]

class RetryableManifestError(Exception):
    """可重试错误基类，与 ManifestIntegrityError（Fatal）严格隔离。"""

class ManifestConflictError(RetryableManifestError):
    """CAS generation 不一致时抛出，调用方可捕获后有界重试。
    ⚠️ 不继承 ManifestIntegrityError，防止 fatal 处理器误捕获。"""
    pass

def mutate_manifest_under_lock(
    workspace_root: str,
    mutator: Callable[[Manifest], Tuple[Manifest, T]],
    *,
    lock_timeout: float = 5.0,  # 取锁超时（非整体操作超时，注释中须区分两者语义）
) -> T:
    """持锁 → 锁内 fresh-fd 重读 → mutator → 兜底 CAS → 原子覆写。
    - mutator 必须为纯函数，禁止调用任何取锁 API（违反 → RuntimeError）；
    - mutator 抛错 → 不落盘、释放锁(finally)、关闭 fd(finally)、原样重抛；
    - 临时文件置于清单同目录（跨平台 os.replace 原子性）。
    """
```

#### 【分步改造指引】
1. 紧接 ManifestIntegrityError 下方（L42 附近）添加 RetryableManifestError 和 ManifestConflictError（勿继承 ManifestIntegrityError）；
2. 实现 mutate_manifest_under_lock：try/except/finally 三段式；锁重入检测 → RuntimeError；临时文件同目录；
3. 五函数提取各自 _mutator 内函数委托调用；保持外部签名、返回类型与异常类型完全不变；
4. 编写 test_dual_process_cas.py：multiprocessing.Barrier 强制交叠；子进程超时 terminate() 回收；
5. 跑测全绿。

#### 【防御与边缘校验】
- 临界区内全新 fd 读盘，杜绝 OS 脏页缓存
- lock_timeout 为取锁超时（文档化），慢 mutator 须注释警告「必须 O(小)」
- ManifestConflictError 不被 except ManifestIntegrityError 误捕获
- mutator 内调用取锁 API → RuntimeError，非死锁
- 外部 API 向后兼容：五函数签名与异常类型不变
- 临时文件与清单同目录（跨平台原子性）

#### 【DoD 验证命令】
```bash
python -m pytest plugins/quench-dev-tasks/server/tests/test_dual_process_cas.py -q
python -m pytest plugins/quench-dev-tasks/server/tests/ -q   # 全量套件，20/20 零回归
```
**必含断言**：并发「恰好一方成功、另一方得可恢复结果」；mutator 抛错后清单哈希不变；重入守护 RuntimeError 非死锁；五函数签名与异常类型不变。

---

### 任务 2.3 ✔️ 已完成 — 租约心跳与防杀探针的失败语义、绑定校验与全量回归加固

> **前置依赖**：须在任务 2.1 完成后开始。

#### 【涉及文件】
```
[MODIFY] plugins/quench-dev-tasks/server/manifest_lease.py
[MODIFY] plugins/quench-dev-tasks/server/reaper.py
[MODIFY] plugins/quench-dev-tasks/server/tests/test_lease_heartbeat_external.py
```

#### 【缺陷根因与修改目标】
```
【根因分析（Reviewer F-2.1-B/C/D/E/F/G/H/I）】
- session_id → holder_token 绑定契约缺失；
- 全部 stat 失败时的 True/False 返回语义未钉死；
- 长计算无 fs 写入（>window_seconds）导致双机制同时失效误杀；
- [NEW] 白名单文件尚未创建时探针恒 False，与上条叠加放大误杀；
- files 路径穿越（../、绝对路径、Windows 盘符）未校验；
- ttl_seconds 无上下界；generation 校验范围在文档中未声明；
- 扫描量无上限，可拖慢 Reaper 主循环。

【修改目标】
1. 核验 session_id 在清单中的存储与唯一性；若无则改为调用方持有 (holder_token, generation)；
2. 白名单缺省纳入父目录 mtime，覆盖 [NEW] 文件冷启动场景；
3. 代码注释 + 任务文档明确：fail-closed 校验在 touch_heartbeat 临界区内同时校验
   holder_token AND generation；
4. 全部 stat 失败时返回 True（保守存活）+ stderr 告警；
5. max_scan 早退。
```

#### 【目标签名与类型契约】
```python
# 钉死版契约（基于任务 2.1 扩展）
def touch_lease_heartbeat(workspace_root: str, session_id: str, ttl_seconds: int) -> bool:
    # 仅当锁内 session_id ∧ holder_token ∧ generation 均一致时续约，否则 False

def is_workspace_actively_modifying(
    workspace_root: str, files: Sequence[str],
    window_seconds: int = 300, *, max_scan: int = 256,
) -> bool:
    # 越界/异常路径 → True（保守存活），路径穿越 → 静默剔除
```

#### 【分步改造指引】
1. 核验并注释声明 session_id 与 holder_token 的一一对应关系（或调整签名）；
2. files 列表末尾追加父目录 mtime 检查点兜底冷启动；
3. 添加路径越界样本断言（../、绝对路径、Windows 盘符被剔除）；
4. 补充 ttl_seconds 越界断言；
5. 新增全部 stat 失败返回 True 专项用例；
6. 新增 max_scan 早退用例。

#### 【防御与边缘校验】
- 全部 stat 失败返回 True + stderr 告警，不抛穿 Reaper 主循环
- ttl_seconds 越界抛 LeaseContractError
- 路径穿越被静默剔除
- 父目录 mtime 兜底冷启动 [NEW] 文件场景

#### 【DoD 验证命令】
```bash
python -m pytest plugins/quench-dev-tasks/server/tests/test_lease_heartbeat_external.py -q
python -m pytest plugins/quench-dev-tasks/server/tests/ -q   # 全量 20/20 零回归
```
**必含断言**：全部 stat 失败返回 True；越界路径被剔除；TTL 越界抛 LeaseContractError；token/generation 任一不符返回 False；max_scan 早退；父目录 mtime 兜底。

---

### 任务 2.4 ✔️ 已完成 — CAS 事务的异常安全、冲突语义与双进程测试确定性加固

> **前置依赖**：须在任务 2.2 完成后开始。

#### 【涉及文件】
```
[MODIFY] plugins/quench-dev-tasks/server/manifest.py
[MODIFY] plugins/quench-dev-tasks/server/tests/test_dual_process_cas.py
```

#### 【缺陷根因与修改目标】
```
【根因分析（Reviewer F-2.2-B/C/D/E/F/G/H/I/J 与跨任务 X-1）】
- ManifestConflictError 若继承 ManifestIntegrityError → fatal 处理器误捕获可恢复冲突；
- mutator 抛异常时无事务闭环（半写/锁悬挂）；
- 临界区持锁期间 mutator 可能调用 touch_heartbeat 引发锁重入死锁；
- CAS 由调用方自证，忘检 generation 时静默覆盖（丢更新）；
- PEP 695 [T] 仅 Python 3.12+，低版本 SyntaxError；
- 双进程用例缺 Barrier 可能假绿，孤儿进程持锁污染后续；
- lock_timeout=5.0 语义歧义；五函数迁移无回归证据；原子替换跨文件系统未校验。

【修改目标】
1. 确认 ManifestConflictError 继承 RetryableManifestError（非 ManifestIntegrityError）；
2. try/except/finally 完善 mutate_manifest_under_lock 异常闭环；
3. 锁重入守护（RuntimeError，非死锁）；
4. helper 内部兜底 CAS（fresh-fd 读 generation，replace 前再断言）；
5. multiprocessing.Barrier 改造双进程用例；
6. 五函数「签名不变 + 异常类型等价」回归断言；
7. 临时文件同目录原子替换边界断言。
```

#### 【目标签名与类型契约】
```python
class ManifestConflictError(RetryableManifestError): ...
# ⚠️ 确认不继承 ManifestIntegrityError

def mutate_manifest_under_lock(
    workspace_root: str,
    mutator: Callable[[Manifest], Tuple[Manifest, T]],
    *, lock_timeout: float = 5.0,
) -> T: ...
# 补充契约：mutator 内调用取锁 API → RuntimeError；抛错 → 磁盘不变 + 锁释放(finally)；
# atomic_replace 前 generation 再断言（兜底 CAS）；临时文件置于清单同目录。
```

#### 【分步改造指引】
1. 核验 ManifestConflictError 继承链为 RetryableManifestError 子类；
2. try/except/finally 封闭 mutate_manifest_under_lock：mutator 抛错 → 不落盘、释放锁、关闭 fd；
3. 临界区加锁重入守护：检测到重入立刻抛 RuntimeError；
4. atomic_replace 前 generation 二次断言（兜底 CAS）；
5. 双进程用例引入 multiprocessing.Barrier；子进程超时 terminate() 回收；
6. 五函数逐一补「签名不变 + 异常类型等价」回归断言；
7. 临时文件固定于清单同目录，加跨平台原子替换边界断言。

#### 【防御与边缘校验】
- mutator 异常 → 磁盘不变 + 锁可再获取（不悬挂）
- mutator 内调用取锁 API → RuntimeError，非死锁
- 子进程异常退出 → 无孤儿持锁
- 跨平台原子替换（临时文件同目录）
- 兜底 CAS 冲突抛 ManifestConflictError（RetryableManifestError 子类）

#### 【DoD 验证命令】
```bash
python -m pytest plugins/quench-dev-tasks/server/tests/test_dual_process_cas.py -q
python -m pytest plugins/quench-dev-tasks/server/tests/ -q   # 全量 20/20 零回归
```
**必含断言**：并发「恰好一方成功、另一方得可恢复结果」；mutator 抛错后清单哈希不变；重入守护 RuntimeError 非死锁；五函数签名与异常类型不变；跨平台原子替换不跨目录。

---