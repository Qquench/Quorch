# 2026-09-24_v1.08_step03_capability_tokens_and_onboarding Development Tasks / 开发任务单

> **Execution Guidelines for AI Models / 执行模型须知**
> - Strictly follow each task's [Step-by-Step Instructions / 分步改造指引] in sequential order
> - Do not modify files outside the declared task scope / 不得修改任务未涉及的文件
> - Preserve all existing comments and docstrings unless explicitly instructed / 保留所有现有注释和文档字符串
> - **Mandatory Unit Test Assertions / 改逻辑必加单测断言**：Append assertions in the test directory to prevent regressions
> - Upon starting a task, update its status to `🔨 执行中`; upon completion, update to `✔️ 已完成`

- **Created Date / 创建日期**：2026-09-24

---

## Task List & Status / 任务清单与状态

### 任务 3.1 ⬜ 待确认 — 环境变量角色权限令牌隔离与服务端只读端点设防

#### 【涉及文件】
```
[MODIFY] plugins/quench-dev-tasks/server/server.py
[MODIFY] plugins/quench-dev-tasks/server/project_config.py
[NEW] plugins/quench-dev-tasks/server/tests/test_capability_tokens.py
```

#### 【缺陷根因与修改目标】
```
【根因分析】
在双宿主架构中，Reviewer 进程拥有与 Runner 相同的 FastMCP 工具清单。若 Reviewer 宿主环境中的高权限模型误触发代码修改或任务推进工具（如 dev_tasks_checkout、dev_tasks_complete），将违反核心不变量 INV-6（Review is read-only）与角色分离纪律。

【修改目标】
1. 在 project_config.py 中解析 QUENCH_CAPABILITY_ROLE 环境变量（'runner' | 'reviewer' | 'full'，默认为 'full' 向后兼容）；
2. 在 server.py 中对所有写操作与跃迁工具（dev_tasks_checkout、dev_tasks_complete、dev_tasks_confirm、dev_tasks_reclaim 等）增加角色权限拦截门禁；
3. 当 role 为 'reviewer' 时，禁止调用除只读查询与咨询之外的任何跃迁/写操作工具，报错返回 PermissionDeniedError；
4. 编写 test_capability_tokens.py 验证角色隔离安全性。
```

#### 【目标签名与类型契约】
```
CapabilityRole = Literal['runner', 'reviewer', 'full']

def enforce_capability_role(required_role: CapabilityRole, current_role: CapabilityRole) -> None:
    # 若当前角色权限不足以执行目标工具，抛出 PermissionDeniedError
```

#### 【分步改造指引】
1. 在 project_config.py 中增加 capability_role 字段并从环境变量 QUENCH_CAPABILITY_ROLE 读取；
2. 在 server.py 工具分发入口处增加角色守卫 enforce_capability_role；
3. 编写 test_capability_tokens.py 断言 reviewer 角色下写操作被硬性拒绝，只读端点正常放行；
4. 跑测确保全绿。

#### 【防御与边缘校验】
默认 QUENCH_CAPABILITY_ROLE 为 'full'，保持本地无配置单端开发体验完全向前兼容
拦截报错信息明确指示当前角色与所需角色，不产生混淆误导
绝不在错误信息中泄漏系统环境变量值或敏感路径

#### 【DoD 验证命令】
```bash
python -m pytest plugins/quench-dev-tasks/server/tests/test_capability_tokens.py -q
```

---

### 任务 3.2 ⬜ 待确认 — 审查简报 SHA-256 签名签发与回填裁决溯源存证

#### 【涉及文件】
```
[MODIFY] plugins/quench-dev-tasks/server/consultation.py
[MODIFY] plugins/quench-dev-tasks/server/handoff_card.py
[MODIFY] plugins/quench-dev-tasks/server/observability_policy.py
[NEW] plugins/quench-dev-tasks/server/tests/test_verdict_provenance.py
```

#### 【缺陷根因与修改目标】
```
【根因分析】
外部拓扑中若由 Runner 代理输入 Reviewer 结论（Paradigm A 人桥模式），容易诱发 Runner 伪造“审查通过”以绕过阻断（触碰 INV-3 反向角色扮演硬停）。此外，裁决审计日志 verdicts.jsonl 缺乏来源真实性元数据（provenance）。

【修改目标】
1. 在 handoff_card.py 中新增简报上下文 SHA-256 数字摘要签发；
2. 在 consultation.py 与 server.py 回填结论时校验该简报签名；
3. 在 observability_policy.py 写入 verdicts.jsonl 时，新增 provenance 字段（'human_attested' | 'external_engine' | 'synthetic_bypass'）；
4. 编写单测验证篡改签名拦截与合规签名放行。
```

#### 【目标签名与类型契约】
```
def generate_card_digest(task_id: str, context_text: str) -> str:
    # 返回标准 SHA-256 签名十六进制字符串

def verify_card_digest(task_id: str, context_text: str, signature: str) -> bool:
    # 校验卡片上下文摘要真实性
```

#### 【分步改造指引】
1. 在 handoff_card.py 中实现 generate_card_digest 与 verify_card_digest 纯函数；
2. 在 render_handoff_card 底部注入签名锚点 <!-- QUENCH-CARD-SIG: <sha256> -->；
3. 在 verdicts.jsonl 审计日志记录器中注入 provenance 字段；
4. 编写 test_verdict_provenance.py 断言摘要校验和防伪能力；
5. 跑测验证 100% 通过。

#### 【防御与边缘校验】
摘要生成对换行符（CRLF vs LF）与首尾空格做严格归一化后再哈希，杜绝跨平台误报
签名不匹配时明确返回 VERDICT_SIGNATURE_MISMATCH 错误并阻断流程
绝不对空摘要或空上下文生成假签名，防止空转漏洞

#### 【DoD 验证命令】
```bash
python -m pytest plugins/quench-dev-tasks/server/tests/test_verdict_provenance.py -q
```

---

### 任务 3.3 ⬜ 待确认 — 主流外部执行器配置样例与无缝接入操作指南

#### 【涉及文件】
```
[NEW] docs/external_runner_onboarding.md
[NEW] examples/external_runners/codex_config.toml
[NEW] examples/external_runners/claude_code_config.json
[NEW] examples/external_runners/aider_launch.sh
```

#### 【缺陷根因与修改目标】
```
【根因分析】
外部执行器用户接入 Quench 时，面临缺乏现成配置样例与标准启动命令的痛点。不同 CLI（Codex、Claude Code、Aider）的 MCP 配置结构各不相同，缺乏统一操作指引容易引发配置错误与踩坑。

【修改目标】
1. 编写 docs/external_runner_onboarding.md，详细说明非对称双宿主架构下的外部执行器配置与协作流程；
2. 提供 Codex CLI（config.toml）、Claude Code（config.json）、Aider（启动脚本）的标准样例配置；
3. 声明各种宿主在缺少 PreToolUse 钩子时依靠 B1 工作树对账与 Pre-commit 守卫的防御机理。
```

#### 【目标签名与类型契约】
```
# 配置样例文本结构与无死链接 Markdown 规范
```

#### 【分步改造指引】
1. 创建 examples/external_runners/ 目录；
2. 编写 codex_config.toml、claude_code_config.json、aider_launch.sh 样例文件；
3. 编写 docs/external_runner_onboarding.md 接入指南；
4. 确保文档与代码样例中无硬编码绝对路径或私有 Token。

#### 【防御与边缘校验】
所有样例配置中的路径均使用占位符 <workspace-root>，杜绝硬编码个人绝对路径
环境变量均使用标准变量名（DEEPSEEK_API_KEY、OPENAI_API_KEY 等），绝不包含任何真实 Token
配置文件遵循跨平台语法规范，脚本赋予标准执行权限说明

#### 【DoD 验证命令】
```bash
python -m pytest plugins/quench-dev-tasks/server/tests/test_architecture_doc_anchor_integrity.py -q
```

---

