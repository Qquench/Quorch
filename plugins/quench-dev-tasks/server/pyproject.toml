[project]
name = "quench-dev-tasks-server"
version = "0.1.0"
description = "FastMCP Server for Quench DevTasks Orchestrator"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0",
    "pyyaml>=6.0",
    "filelock>=3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
